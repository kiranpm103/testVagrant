step 1: run the pom.xml file so that it will download all dependency
step 2: Go to src --> test --> interview --> RcbTeamPlayerValidation class
step 3: run the class

note: we are using the helper class (BaseClass), it will read the json file, in main class we are extending BaseClass
